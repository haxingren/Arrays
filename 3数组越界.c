//#define _CRT_SECURE_NO_WARNINGS
//
//#include <stdio.h>
//
//int main()
//{
//	//已知数组的下标是有界限的，即一个有n个元素的数组，其下标的范围是0~n-1
//	//如果下标超出或小于这个范围，那就是数组越界访问了，超出了数组非法空间的访问
//	//并且对于这种情况编译器不一定会报错，但是程序运行时还是会出现问题，使用写代码时须自行注意
//	return 0;
